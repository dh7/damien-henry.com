# Required: Your public Notion page ID
NEXT_PUBLIC_NOTION_PAGE_ID=your-notion-page-id

# Recommended: Secret for on-demand revalidation (server-side only)
# Generate with: openssl rand -hex 32
REVALIDATE_SECRET=your-secret-here

# Optional: Your production site URL (for sitemap generation)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: For triggering full rebuilds
REBUILD_SECRET_TOKEN=your-rebuild-secret
VERCEL_DEPLOY_HOOK_URL=your-vercel-deploy-hook-url

# Required for AI chat functionality
GOOGLE_GENERATIVE_AI_API_KEY=your-google-api-key

# Optional: Enable chat message logging (default: disabled)
ENABLE_CHAT_LOGGING=false

# Optional: Enable page view logging (default: disabled)
ENABLE_PAGE_LOGGING=false

# Required if chat or page logging is enabled: Password for admin messages page
ADMIN_PASSWORD=your-admin-password

# Optional: Redis URL for usage tracking (required if logging is enabled)
dh_usage_REDIS_URL=your-redis-url

